# cppevent
C++ equivalent-ish of .NET event

## Why use this?

Because:

- Hedaer only
- Thread Safe
- `cppevent::event` allows for in-class raise only (just like in C#)
- C#-like interface: `+=`, `-=`, `()`
